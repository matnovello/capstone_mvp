{
  game_id: @game.id,
  room: {
    current_room: @game.current_room,
    has_monster?: @game.rooms.last.has_monster?,
  },

  user: {
    name: @game.user.name,
    base_health: @game.user.base_health,
  },
  monster: {
    name: @game.rooms.last.blob_monster.name,
    catch_phrase: @game.rooms.last.blob_monster.catch_phrase,
    max_health: @game.rooms.last.blob_monster.max_health,
    attack: @game.rooms.last.blob_monster.attack,
    defense: @game.rooms.last.blob_monster.defense,

  },
}
