{
  game_id: @game.id,
  room: {
    current_room: @game.current_room,
    has_monster?: @game.rooms.last.has_monster?,
  },

  user: {
    name: @game.user.name,
    base_health: @game.user.base_health,
  },
  
  FIGURE OUT HOW TO USE CONDITIONALS IN JSON AHAHGHGHGHGHGHGHGGGGHGHHHHHH SO ANNOYING     
  if( @game.rooms.last.has_monster? === true)                           
    {
    message: "hello"
    },
end
}
